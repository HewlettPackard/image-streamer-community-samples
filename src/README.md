# Artifact bundle sources directory

## Description

One directory per artifact bundle sources. Each directory contains a `BuildPlans` and `PlanScripts` subdirectories with their respective sources.
